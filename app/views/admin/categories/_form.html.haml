= simple_form_for([:admin, @category]) do |f|
  = f.error_notification

  .form-inputs
    = f.select(:category_type, Category::TYPES)
    = f.input :name

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    |
    = link_to 'Back', admin_categories_path, class: 'btn'

